version: '3'

services:
  nest-api:
    image: ghcr.io/virtual-academic-affair/nest-api:latest
    container_name: nest-api-container
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env.production

networks:
  nest-network:
    driver: bridge
